{% extends 'base.html.twig' %}

{% block title %}Service
{% endblock %}

{% block body %}
	<div class="container" style="max-width: 80rem;">
		<h1 class="mt-3">Service</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ service.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ service.nom }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ service.description }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-primary me-2" href="{{ path('app_service_index') }}">Retour à la liste des services</a>

		<a class="btn btn-primary me-2" href="{{ path('app_service_edit', {'id': service.id}) }}">Modifier</a>

		{{ include('service/_delete_form.html.twig') }}
	</div>
{% endblock %}
